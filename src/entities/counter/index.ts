import { ADAPTERS, USE_CASES } from './application';
import { IMPLEMENTATIONS, VIEW_MODEL } from './infrastructure';

/**
 * Represents the counter entity in the application.
 */
export const COUNTER_ENTITY = {
  ADAPTERS,
  USE_CASES,
  IMPLEMENTATIONS,
  VIEW_MODEL,
};

export * from './domain';
